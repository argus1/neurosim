package
{
    import flash.events.MouseEvent;
	import flash.display.*;
    
	public class Main extends MovieClip
    {
        public function Main()
        {
             trace( "Yep, it's working" );
        }
		public function Tester()
        {
             trace( "and so is this" );
        }
    }
	
	
}
